<nav class="navbar navbar-light d-flex">
  <div class="d-flex" id="navbar-right">
    <i class="fas fa-film"></i>
    <%= link_to 'Watchlist', root_path, class: 'navbar-brand' %>
  </div>
  <div class="d-flex">
    <% if defined?(@list) && @list[:id] %>
      <div class="d-flex">
        <%= link_to 'Delete List', list_path(@list), method: :delete, class: 'btn btn-outline-danger my-2 my-sm-0 ml-2', data: { confirm: 'Are you sure you want to delete this list?' } %>
        <%= link_to 'Add List', new_list_path, class: 'btn btn-outline-primary my-2 my-sm-0 ml-2'%>
      </div>
    <% else %>
      <div>
        <%= link_to 'Add List', new_list_path, class: 'btn btn-outline-primary my-2 my-sm-0 ml-2'%>
      </div>
    <% end %>
  </div>
</nav>
